const initialState = {
  isFetching: false,
  failure: false,
  errMessage: "",
  data: []
};
export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
